---
name: direct-paper-translator
description: 极简学术 PDF 直接翻译。用于用户希望让 OpenCode 快速预处理具有文字层的 PDF、一次性完整翻译成带译者导读的简体中文 Markdown，并立即套用固定模板生成 HTML；不运行 OCR、图片处理、脚注强匹配、多轮 QA、事实门禁、并行 Worker 或自动返工。
---

# 直接论文翻译

只执行以下三步，不增加其他流程。

## 1. 快速预处理

以本 `SKILL.md` 所在目录为工作目录，直接运行下列命令。不要先用 `ls`、`stat` 或 `pdfinfo` 检查输入：

`python3 scripts/flow.py prepare "论文.pdf" --jobs-root "任务目录"`

脚本只读取 PDF 现有文字层、规范空行并限制超长行。文字层不足时直接报告，禁止启动 OCR。

## 2. 直接翻译 Markdown

只读取返回的 `source_markdown`，立即按原顺序完整翻译到 `translation_markdown`。`prepare` 返回的页数与字符数就是本次文字层范围；不要因论文印刷页码而猜测缺页，也不要用 `wc`、`tail`、`cat` 或第二次读取复核。`translation_markdown` 只允许执行一次完整 `Write`：写入成功后禁止自审、重写或润色，立即进入 `finalize`。源文开头或结尾即使停在半句话，也只译到现有文字为止，不添加“继续”“缺失”或自行补写的内容。不得阅读脚本、另写计划、建立索引或运行其他工具；译文实际写完前不得结束。

Markdown 必须：

- 使用简体中文，保留原文中的必要专名、公式、引文和书目信息；
- 如果 `source_markdown` 中存在 `〖原页 N〗` 之类的预处理页码标签，写入译文时删除，不保留也不添加替代页码标签；
- 以 `# 中文标题` 开头，并写 `作者：...`、`来源：...`；
- 包含 `## 译者导读`，下设 `### 主要内容`、`### 研究方法`、`### 存在的缺陷与局限`、`### 值得关注的地方`；
- 完整翻译正文。注释和参考文献按源文顺序直接翻译或保留，不做强制匹配。

## 3. 生成 HTML

译文写完后立即运行：

`python3 scripts/flow.py finalize "job_dir"`

`finalize` 返回成功后立即交付其中的 Markdown 与 HTML 路径，不要再用 `ls` 或其他命令检查目录。不要追加 OCR、图片、脚注匹配、术语表、事实检查、多轮校验、Reviewer、并行翻译或自动返工。
